__pycache__/
*.pyc
*.pyo

.venv/
venv/

bin/
logs/

.test_*/
*.db
*.db-journal
*.db-wal
*.db-shm

.claude/
.vscode/
.idea/

# Secrets / local config — NEVER commit. The canonical store lives at
# ~/vortex/config.json (outside the repo); these patterns just guard
# against a copy ever landing inside the tree.
.env
.env.*
!.env.example
config.json
vortex-config.json
*.token
mynotes.md.txt
