<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="12dp"
    android:background="?android:attr/selectableItemBackground">

    <!-- B11.5: simple file-row icon (folder vs file) drawn inline so
         we don't ship per-mime asset bloat. Color tinted in code. -->
    <TextView
        android:id="@+id/file_icon"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="@color/vortex_cyan"
        android:fontFamily="monospace"
        android:text="•"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="10dp">

        <TextView
            android:id="@+id/file_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/vortex_text"
            android:textSize="14sp"
            android:singleLine="true"
            android:ellipsize="middle"/>

        <TextView
            android:id="@+id/file_meta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="11sp"
            android:textColor="@color/vortex_text_muted"
            android:layout_marginTop="2dp"/>
    </LinearLayout>
</LinearLayout>
