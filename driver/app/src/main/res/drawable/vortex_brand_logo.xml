<?xml version="1.0" encoding="utf-8"?>
<!-- B10: webapp `.brand .logo` look. Purple->cyan gradient square with
     a dark center cut-out via layer-list (matches the CSS ::after rule
     that paints `var(--bg)` inset 5px from the gradient edge). -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="135"
                android:startColor="@color/vortex_purple"
                android:endColor="@color/vortex_cyan" />
            <corners android:radius="8dp" />
        </shape>
    </item>
    <item
        android:top="5dp"
        android:bottom="5dp"
        android:left="5dp"
        android:right="5dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/vortex_bg" />
            <corners android:radius="4dp" />
        </shape>
    </item>
</layer-list>
